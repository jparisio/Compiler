/* Test Case 1: no errors */

void main(void) {
    int a[5]; 
    int b;
    bool flag;

    b = 10;
    flag = b > 5;

    if (flag) {
        a[0] = b;
    }

    while (b > 0) {
        b = b - 1;
    }

    output(a[0]);
}
